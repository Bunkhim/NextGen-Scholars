// Template for Secrets.xcconfig — copy to Secrets.xcconfig and fill in real values.
// NEVER commit Secrets.xcconfig itself (it's in .gitignore).
GOOGLE_IOS_CLIENT_ID = REPLACE_ME
FACEBOOK_APP_ID = REPLACE_ME
FACEBOOK_CLIENT_TOKEN = REPLACE_ME
